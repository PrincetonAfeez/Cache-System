{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/cachelab/schema/cache_config_toml_container.schema.json",
  "title": "CacheLab TOML Configuration Container",
  "description": "Schema for the full TOML document after conversion to JSON. CacheLab accepts either a top-level config or a [cache] table.",
  "oneOf": [
    { "$ref": "cache_config.schema.json" },
    {
      "type": "object",
      "additionalProperties": false,
      "required": ["cache"],
      "properties": {
        "cache": { "$ref": "cache_config.schema.json" }
      }
    }
  ]
}
